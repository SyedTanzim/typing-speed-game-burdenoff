generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "debian-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// Account record. The password field stores a bcrypt hash, never plain text.
model User {
  id        String       @id @default(uuid())
  email     String       @unique
  password  String
  createdAt DateTime     @default(now())
  results   GameResult[]
}

/// One completed game owned by a User through the userId foreign key.
model GameResult {
  id            String   @id @default(uuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])
  timeSeconds   Float
  correctChars  Int
  wrongAttempts Int
  penaltyTime   Float
  createdAt     DateTime @default(now())

  @@index([userId])
  @@index([timeSeconds])
}
